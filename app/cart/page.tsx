import Cart from '@/pages/Cart';

export const metadata = {
  title: 'Urban Village | Cart',
  description: 'View your selected produce, update quantities, and proceed to checkout with Urban Village’s seasonal basket.'
};

export default function Page() {
  return <Cart />;
}
