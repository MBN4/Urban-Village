import Shop from '@/pages/Shop';

export const metadata = {
  title: 'Urban Village | Shop',
  description: 'Browse our seasonal harvest and curated farm bundles. Fresh produce is available for fast delivery across the region.',
};

export default function Page() {
  return <Shop />;
}
